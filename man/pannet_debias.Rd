% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/debias.R
\name{pannet_debias}
\alias{pannet_debias}
\title{Split-panel jackknife bias correction for dynamic pannet fits}
\usage{
pannet_debias(formula, data, id, time = NULL, ..., variables = NULL)
}
\arguments{
\item{formula, data, id, time, ...}{passed through to three separate
\code{\link[=pannet]{pannet()}} calls (on the full panel and on each time-split half); \code{id}
and \code{time} follow the same bare-column-name convention as \code{pannet()}.
\code{model} must be \code{"dynamic"}; \code{time} is required (it defines the split).}

\item{variables}{character vector of covariates to compute bias-corrected
marginal effects for (via \code{\link[=marginal_effects]{marginal_effects()}}). Defaults to every
non-id/time variable on the right-hand side of \code{formula}.}
}
\value{
a list with \code{corrected} (named numeric vector, the bias-corrected
average marginal effects), \code{full}/\code{half1}/\code{half2} (the uncorrected
marginal effects each fit produced), and \code{fit_full}/\code{fit_half1}/
\code{fit_half2} (the three underlying \code{pannet} fits)
}
\description{
Any fixed-effects-style estimator with a lagged dependent variable is
inconsistent at finite T (Nickell, 1981); \code{pannet(model = "dynamic")} is
not exempt (see \code{vignette("pannet-validation")}'s dynamic-panel section).
Because \code{pannet} is a neural architecture, no closed-form bias correction
analogous to the LSDV bias formulas exists. This applies a model-agnostic
alternative instead: the split-panel jackknife (Dhaene & Jochmans, 2015).
}
\details{
The panel is split into two half-panels by time, \code{model = "dynamic"} is
fit separately on each half and on the full panel, and the (average)
marginal effect of each variable is bias-corrected as

\code{theta_corrected = 2 * theta_full - 0.5 * (theta_half1 + theta_half2)}

This is an outer wrapper around three ordinary \code{\link[=pannet]{pannet()}} calls; it does
not modify the training loop itself.
}
