% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark.R
\name{pannet_benchmark}
\alias{pannet_benchmark}
\title{Benchmark pannet against classical panel estimators}
\usage{
pannet_benchmark(
  formula,
  data,
  id,
  time,
  family = "gaussian",
  test_data = NULL,
  test_fraction = 0.2,
  pannet_configs = NULL,
  epochs = 300L,
  seed = 42L,
  verbose = FALSE
)
}
\arguments{
\item{formula}{Model formula.}

\item{data}{Training data frame.}

\item{id}{Bare name of the unit identifier column.}

\item{time}{Bare name of the time identifier column.}

\item{family}{Outcome family.}

\item{test_data}{Test data frame (must include the response). If \code{NULL},
\code{test_fraction} of individuals are held out from \code{data}.}

\item{test_fraction}{Fraction of individuals to hold out when \code{test_data}
is \code{NULL}.}

\item{pannet_configs}{A named list of extra argument lists forwarded to
\code{pannet()}. Defaults to a grid of pooled / individual / twoway models.}

\item{epochs}{Number of training epochs for each pannet configuration.}

\item{seed}{Random seed.}

\item{verbose}{Whether to print pannet training progress.}
}
\value{
A data frame with one row per estimator and columns for test metric
and training time.
}
\description{
Fits multiple pannet configurations and, if available, classical estimators
(\code{plm} for Gaussian, \code{pglm} for binary/count), then reports test-set
performance metrics.
}
